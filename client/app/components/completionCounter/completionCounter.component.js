import template from './completionCounter.html';
import controller from './completionCounter.controller';
import './completionCounter.scss';

let completionCounterComponent = {
  bindings: {},
  template,
  controller
};

export default completionCounterComponent;
